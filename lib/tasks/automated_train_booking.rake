desc 'Book a train for Millie on trainline.fr'
task automated_train_booking: :environment do
    # reservation = TrainBooking.last
    # bot = BotBooking.new(reservation)
    # bot.flow
    puts "almost ready son 💩"
    sleep 2
end
